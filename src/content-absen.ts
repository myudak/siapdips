import { createHelperDefault } from "@/lib/content_absen";

// START

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore
Toastify({
  text: "Siap DiPS ~> Absen (●ˇ∀ˇ●)",
  duration: 3000,
  close: true,
  position: "left",
}).showToast();

console.log("Hello Undiplearnv 5");

// START

//  ++++++++++++++++++++++++++++++++++++++++++++++++
// window.addEventListener("load", () => {
createHelperDefault();
// });
